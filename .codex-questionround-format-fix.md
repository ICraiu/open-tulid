Fix the remaining canonical QuestionRound answer-format defect. Work only in `/home/rawsteel/repo/open-tulid`. Do not touch the Wealthy Scholar repo/vault/runtime; do not commit or push.

The prior repair correctly links the generated QuestionRoundFile to its derived QuestionRound and injects it as `Canonical QuestionRound Answers` into the ReviewAnswers prompt. A live replay still failed because the saved answer artifact uses this valid human-readable format:

```markdown
## 1. Who should the first version serve?

**Answer:** Individual builders.
```

But `answers-review.agent.md` says to treat only text below literal `Your answer:` labels as user answers. The worker therefore declared the canonical artifact blank and generated another erroneous follow-up round.

Root requirement: Once a QuestionRoundFile is marked in the prompt as the sole canonical answer record, its responses are authoritative regardless of whether individual answers are written as `Your answer:`, `**Answer:**`, or equivalent explicit response labels. The stale generated task body remains a question template and must never override or cause the canonical responses to be ignored.

Do not attempt a broad Markdown NLP parser or relax provenance/conflict safeguards. Establish a small explicit supported-label policy, document it in the relevant agent/prompt contract, and make the rendered canonical context unambiguous to the worker.

Testing:
1. Add a RED regression using the exact `**Answer:** Individual builders.` canonical artifact format, plus a stale blank template body.
2. Assert the rendered ReviewAnswers prompt both includes the response and explicitly directs the worker to treat it as the user answer despite label formatting.
3. Keep/add the literal `Your answer:` case too.
4. Run focused tests and the full suite.

Report root cause, exact supported-label policy, changed files, and test commands/results.
