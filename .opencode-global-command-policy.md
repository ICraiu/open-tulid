Implement this policy change in `/home/rawsteel/repo/open-tulid` and the Wealthy Scholar task definitions under `/home/rawsteel/repo/obsidian/wealthy-scholar`.

Do not start or stop Tulid, its runtime, scheduler, workers, proxy, Docker containers, or any model service. Do not modify Wealthy Scholar application source. Do not commit or push.

Policy: verification commands are GLOBAL at the project level. There must be no per-task command/check list generated by Codex. Every implementation task inherits the same global project test/build commands. A task may add tests as part of its implementation, and those tests become part of the project test suite, but the task does not define a separate validation command. Completion acceptance runs the global commands and requires all of them to pass.

Implement this uniformly across the task model, prompt generation, workflow, completion/verifier, templates, and task-generation logic. Remove or deprecate per-task run/check/acceptance command fields from newly generated implementation tasks. Preserve ordinary task requirements and test-writing instructions, but do not turn them into per-task contracts or command lists. Make generated task files use one consistent structure carrying product reasoning, technical context, canonical answers, objective, scope, requirements, non-goals, and test expectations as prose/structured task context—not validation commands.

Update Wealthy Scholar implementation task definitions, especially task 8, so incorrect per-task commands are removed; project global commands remain authoritative. Add tests proving global commands are inherited and executed for every implementation task, task-local command lists are ignored/rejected for new tasks, and tasks can add tests picked up by the global suite. Run focused and full deterministic tests. Write a concise implementation summary when done.

Use a fresh plan-first approach: inspect current state, write a short plan, then implement exactly that plan. Do not broaden scope.