Repair the current live Tulid contract-validation blocker for Wealthy Scholar task #7, then leave the tracker ready for local implementation. Work in `/home/rawsteel/repo/open-tulid` and narrowly in the Wealthy Scholar tracker only as needed; do not modify Wealthy Scholar app source and do not commit/push.

Live failure:
- project `wealthy-scholar`, task #7: `Extract the backend composition root without breaking existing APIs`
- job `01M1EZJW0E7A7BZB310QTWP0XP`, transition `PrepareExecutionContract`
- completion validator rejected the generated contract because `checks.focused` contained empty `argv` values; `requirements`/`invariants` included empty values; and prose was supplied to `checks.invariants` where only registered invariant IDs are valid.
- scheduler is degraded after this rejection.

Required:
1. Diagnose whether this is a Codex contract-authoring prompt/template defect, a contract parser/validator ergonomics defect, or a task-breakdown artifact defect.
2. Repair the root cause so execution contracts contain concrete runnable commands and only registered invariant IDs. Do not weaken validation or accept prose as invariant IDs.
3. Repair/recreate task #7’s contract using the existing repo and its real deterministic test commands. Keep scope exactly to task #7.
4. Add regression coverage for this failure mode: an implementation contract with empty argv or prose invariant must be rejected before scheduling, while the corrected contract passes and schedules ImplementTask.
5. Restore the scheduler-ready state but do not perform app implementation yourself; parent will start/monitor the local worker.
6. Run focused and full tests; report exact results.
