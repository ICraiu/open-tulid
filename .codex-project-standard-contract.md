Implement a project-level standard execution contract for Tulid, then migrate the live Wealthy Scholar project and its generated implementation tasks to use it. Work in `/home/rawsteel/repo/open-tulid` and `/home/rawsteel/repo/obsidian/wealthy-scholar`. Do not modify Wealthy Scholar application source. Do not commit/push.

User direction: per-task contracts should be additive refinements, not repeatedly reconstruct global rules. The project’s standard contract belongs in configuration (preferably project `workflow.yaml`, `acceptance.yaml`, or a clear project-owned contract config referenced by them). Each task should inherit the standard and only declare task-specific scope/interfaces/requirements/check additions.

The current repeated failures come from LLM-authored per-task contracts emitting blank `argv`, prose invariants, inconsistent permission setup, and `opencode.json` appearing outside allowed change surfaces.

Required:
1. Design and implement a formal project-level standard contract mechanism in Tulid config/schema/runtime. It must establish globally for a project:
   - worker/container ownership (`--user 1000:1000`) and runtime-home placement for OpenCode config (never project root);
   - allowed baseline change surfaces and forbidden generated artifacts;
   - deterministic baseline commands: backend clean install, backend tests, and project acceptance profile;
   - invariant-ID policy (only registered IDs; no prose);
   - default retry behavior for contract/verification failures with bounded attempts and visible feedback;
   - validation: fail project validation before runtime if the standard contract is malformed.
2. Define merge semantics: standard contract is authoritative; per-task contract may only add/narrow allowed surfaces and add task-specific checks/requirements. It must not blank/remove/replace the project safety/verification baseline. Conflicts must reject deterministically.
3. Update contract-author prompts so the LLM no longer re-specifies global rules, only task delta. Update task #7 and the queued Wealthy Scholar tasks as necessary to conform.
4. Add focused tests plus a real end-to-end test proving: a project standard contract is loaded; a minimal task delta produces a complete valid execution contract; local worker runs under correct UID; no root `opencode.json`; verification baseline runs; a rejected completion receives bounded retry feedback and can be accepted.
5. Run focused and full Tulid tests, then validate the live Wealthy Scholar vault. Do not start the runtime; parent will verify and start it.

Execution boundary: Do NOT start or stop Tulid. Do NOT start any Tulid runtime, worker, proxy, model agent, Docker worker, or DeepSeek request. Only inspect and modify the codebase, run deterministic tests/validation, and leave runtime state untouched. Report the exact config location and inheritance semantics.
