Fix the remaining Tulid multi-round QuestionRound context-chain defect, then repair the current Wealthy Scholar tracker to continue into implementation. Work primarily in `/home/rawsteel/repo/open-tulid`; you may make narrowly necessary tracker-state repairs in `/home/rawsteel/repo/obsidian/wealthy-scholar`. Do not edit Wealthy Scholar app source. Do not commit/push.

The product owner has answered ALL substantive questions and explicitly says: continue without further clarification. The current goal is to reach concrete implementation tasks and have the local DeepSeek worker begin implementation.

Persisted canonical answer records, in chronological order:
- `artifacts/2/QuestionRoundFile/initial-questions.md` — Round 1 answers
- `artifacts/3/QuestionRoundFile/follow-up-questions.md` — Round 2 answers
- `artifacts/4/QuestionRoundFile/follow-up-questions.md` — Round 3 answers
- `artifacts/5/QuestionRoundFile/follow-up-questions.md` — final answer: Phase 0 passes when an automated rubric judge finds a well-documented, actionable plan grounded in sufficient cited research evidence; it is a research-thesis gate, not proof of demand.

Reproduction: task #6 reviewed its canonical final answer and then generated task #7 repeating questions 1–6 from Round 1. This shows the ReviewAnswers prompt only receives the current task/immediate parent context, not all ancestor QuestionRounds’ canonical answer artifacts. This is not legitimate follow-up: every repeated decision has a prior explicit answer.

Required behavior:
1. For a QuestionRound review, collect all ancestor QuestionRounds’ canonical QuestionRoundFile artifacts in order, deduplicate by artifact identity/content, and inject them as explicit user-answer history. The current round’s linked file is canonical for that round; its ancestor answer records must also be available.
2. Preserve precedence: later explicit answers override earlier conflicting answers. Do not silently merge two current canonical artifacts for the same round; keep existing conflict rejection.
3. Add a true regression test with at least three derived rounds: Round 3’s review prompt must contain answer text from Rounds 1, 2, and 3, and must prevent re-asking a Round 1 question.
4. Make the runtime policy unequivocal: given this product owner’s explicit decision to proceed, agents must not create another question round that repeats a settled question. A genuinely new blocking question may only be produced if no earlier round answers it.
5. Repair Wealthy Scholar tracker state: delete/neutralize the erroneous task #7 and its generated artifacts, preserve all prior answer artifacts, make the current final answered round reviewable against the entire ancestor chain, and do not ask the user to repeat anything.
6. Run focused tests and full suite. Report exact root cause/files/tests.

After your repair, do not leave Tulid running yourself. The parent agent will start it and watch it until a concrete ImplementationTask starts via the local `peon-llm` / DeepSeek worker.
