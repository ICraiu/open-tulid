Fix the six failing operator-runtime CLI tests in `/home/rawsteel/repo/open-tulid`.

Use a fresh context. First inspect the current git diff and the runtime lifecycle code. Preserve valid existing work; do not reset or discard changes. Do not start or stop Tulid, any runtime, worker, proxy, or model. Do not launch Docker workers. Only edit source/tests/docs and run deterministic tests. Do not commit or push.

Current failures in `tests/test_operator_cli.py`:
1. `test_runtime_status_reports_running_processes`: simulated scheduler+proxy PIDs are treated as degraded because proxy_running is false.
2. `test_runtime_status_without_project_reports_all_projects`: same proxy-detection failure for multiple projects.
3. `test_runtime_stop_signals_processes_and_removes_state`: proxy stop event is missing.
4. `test_runtime_stop_without_project_stops_all_projects`: proxy stop event is missing.
5. `test_runtime_stop_waits_for_scheduler_before_proxy_decision`: waits only for scheduler PID, not proxy PID.
6. `test_runtime_stop_refuses_to_claim_success_when_proxy_does_not_exit`: returns success instead of exit code 1.

Diagnose the common regression in runtime process identity/reconciliation. Do not weaken process identity checks. The fix must preserve the newer safeguards for stale PID reuse, command matching, start-time matching, and dead-process reconciliation while making the operator CLI correctly recognize and stop the model proxy represented by its runtime state.

Requirements:
- Add/repair focused tests for both valid simulated state and stale/mismatched process state.
- Preserve correct scheduler-before-proxy shutdown ordering.
- Emit proxy-stop status/events when the proxy actually exits.
- Return nonzero when scheduler or proxy remains alive after stop.
- Keep state-file cleanup/reconciliation correct.
- Run all `tests/test_operator_cli.py`, the relevant runtime tests, then the full suite.
- Report root cause, exact files changed, and real results.
