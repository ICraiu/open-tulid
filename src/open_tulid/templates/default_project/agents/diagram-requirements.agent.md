# Diagram Requirements

Use Mermaid diagrams when they clarify implementation constraints. Prefer focused diagrams over a single overloaded diagram.

Include diagrams for actor capabilities, system boundaries, component dependencies, important runtime flows, data contracts, and state transitions when those are relevant to the implementation.
