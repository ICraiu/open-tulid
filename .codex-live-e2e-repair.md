Make the Tulid local implementation loop work END TO END in the live Wealthy Scholar project. Work in `/home/rawsteel/repo/open-tulid` and, only where necessary to repair/retry the live task, `/home/rawsteel/repo/obsidian/wealthy-scholar` plus task #7's Tulid workspace. Do not modify unrelated Wealthy Scholar app scope. Do not commit/push.

User requirement: start a local DeepSeek (`peon-llm`) implementation task, have it respond to verifier feedback, resubmit successfully, have Tulid accept the task, and verify the implemented application/tests. Do not stop at unit tests or a plausible state flag.

Current live failure history:
- task #7: `Extract the backend composition root without breaking existing APIs`
- local worker job `01M1F0FG3Y0K2BTKXJN643N9YH`
- worker submitted code; verifier rejected it because an injected `opencode.json` was outside the permitted change surface and `npm ci --prefix backend` hit EACCES on container-created node_modules.
- first retry implementation changed runner behavior but the worker exited with return code 1 and no active worker container remained; Tulid still marked the job `running`.
- runtime stop marked it orphaned.

Fix all root causes. Required acceptance:
1. **Dead worker reconciliation:** No exited/absent worker container may leave a job `running`. Persist the exit/terminal state and schedule/requeue according to retry policy; make this observable.
2. **Rejected completion feedback loop:** A verifier-rejected completion for a repair-eligible contract failure keeps or resumes a worker with structured feedback, bounded attempts, and a clean workspace. Environment failures must be classified separately and repaired by the runtime, not blindly retried by the model.
3. **Workspace ownership/environment:** Local-worker containers must not create root/node-owned files that make deterministic `npm ci` fail. `opencode` config must live under Tulid runtime home, never project root/change surface.
4. **Live task #7:** Remove stale generated forbidden files, create a clean/reproducible workspace, and retry the exact task using the local DeepSeek worker. Preserve valid prior code only if it is within contract and passes tests.
5. **True E2E regression test:** simulate local worker -> rejected completion -> verifier feedback/retry -> corrected completion -> accepted task. It must assert no stuck running job after process exit and verify acceptance.
6. Run focused tests and full Tulid suite.
7. Then actually run/restart the live Wealthy Scholar Tulid runtime and watch task #7 through **accepted completion**. Verify via task state, accepted completion event, relevant deterministic backend tests, and no lingering worker/container. If a real external blocker remains, report exact evidence, but do not call it done otherwise.

Do not answer with a plan; implement, test, and verify the live end-to-end behavior.
