Fix the live stale-agent-instruction problem that caused Wealthy Scholar QuestionRound reviews to ignore the repaired canonical answer format.

Work primarily in `/home/rawsteel/repo/open-tulid`. You may make the narrowly necessary project-configuration migration/fix under `/home/rawsteel/repo/obsidian/wealthy-scholar`, but do not edit the Wealthy Scholar application code or user answers. Do not commit or push.

Evidence from the live ReviewAnswers prompt:

- It correctly included the canonical answer artifact at lines 314+:
  `# Canonical QuestionRound Answers: artifacts/2/QuestionRoundFile/initial-questions.md`
  with real answers such as `**Answer:** Individual builders.`
- It also included the OLD project-local instruction at line 174:
  `Treat text beneath each \`Your answer:\` label as the user's answer.`
- The current source template at `src/open_tulid/templates/default_project/agents/answers-review.agent.md` was fixed to accept canonical `Your answer:`, `Answer:`, and `Response:` labels with optional Markdown emphasis, but existing project-local copies were never updated.
- The worker therefore produced an invalid repeat follow-up that says every one of the 30 questions was unanswered. This is not a legitimate Round 2.

Required:
1. Diagnose the source/template-versus-existing-project agent lifecycle and implement the smallest safe fix. The active Wealthy Scholar project must use the current canonical-answer policy.
2. Existing project-specific agent customizations must not be overwritten silently. Define an explicit migration/versioning/override precedence rule and surface conflicts or required upgrade action.
3. Add focused regression coverage proving a project generated before the template change does not silently run stale answer-review instructions, or that the runtime deterministically injects the current canonical-answer contract as binding policy regardless of stale project-local wording.
4. Fix the current Wealthy Scholar tracker state only as needed: preserve Round 1 answers, delete/neutralize the duplicate erroneous follow-up, and leave the original Round 1 ready for a correct re-review. Do not advance past the review automatically.
5. Run focused tests and full suite. Report root cause, exact active-project migration behavior, files changed, and test results.
