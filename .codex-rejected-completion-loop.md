Fix a live Tulid execution-loop failure in `/home/rawsteel/repo/open-tulid`. Do not modify Wealthy Scholar application code. You may inspect the tracker and worker workspace at `/home/rawsteel/repo/obsidian/wealthy-scholar` and `/home/rawsteel/.tulid/workspaces/01M1F0FG3Y0K2BTKXJN643N9YH` as evidence. Do not commit/push.

Product-owner correction: a Tulid implementation worker must CONTINUE after a completion submission is rejected. It receives the verifier feedback, fixes the problem, and resubmits. Only an ACCEPTED completion terminates that local worker/task loop.

Live failure:
- Task #7, job `01M1F0FG3Y0K2BTKXJN643N9YH`, worker `peon-llm` (local DeepSeek)
- Worker submitted code and test evidence.
- Verification rejected due to a forbidden added `opencode.json` and `npm ci --prefix backend` EACCES in workspace node_modules.
- GPU is now idle; job is `completion_rejected`; no feedback/retry loop is running. Task is stranded in ReadyToImplement.

Diagnose the actual state-machine / executor / completion-endpoint failure. Do not assume the worker stopped merely because it submitted completion. Determine exactly why verifier feedback did not keep the worker alive or requeue it.

Required:
1. Add a tight regression test reproducing: local worker submits a rejected completion, receives structured verifier feedback, makes a scoped correction, resubmits, and reaches accepted completion without manual intervention.
2. Fix the root cause in Tulid. Do not weaken verifier checks, auto-accept failed work, or treat rejected completion as task completion.
3. Make retry behavior bounded and observable: preserve attempt count, verifier feedback, retry reason, and clear terminal failure after the configured limit.
4. Repair the live task #7 state/workspace so the local worker is able to continue from verifier feedback. The valid code changes already attempted should not be casually discarded; remove only the forbidden generated file and resolve the deterministic workspace install permission issue through the correct execution/environment mechanism.
5. Run focused tests and full suite. Report root cause, live repair action, changed files, commands/results.
